import React from 'react';
import { ArtistFormClient } from './ArtistFormClient';

export const metadata = {
  title: 'Alta de Artista | GUTA CMS',
};

export default function NuevoArtistaPage() {
  return <ArtistFormClient />;
}
